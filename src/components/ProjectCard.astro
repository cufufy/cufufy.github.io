---
import type { Project } from '../data/projects';

interface Props {
  project: Project;
}

const { project } = Astro.props;
---

<div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow border border-gray-100">
  <div class="p-6">
    <div class="flex justify-between items-start mb-2">
      <span class="inline-block bg-indigo-100 text-indigo-800 text-xs px-2 py-1 rounded-full font-semibold uppercase tracking-wide">
        {project.category}
      </span>
      {project.featured && (
        <span class="text-yellow-500" title="Featured Project">★</span>
      )}
    </div>
    <h3 class="text-xl font-bold text-gray-900 mb-2">
      <a href={project.link} class="hover:text-indigo-600 transition-colors" target="_blank" rel="noopener noreferrer">
        {project.name}
      </a>
    </h3>
    <p class="text-gray-600 mb-4">{project.one_liner}</p>

    <div class="mb-3">
      <h4 class="text-sm font-semibold text-gray-900">Outcomes:</h4>
      <p class="text-sm text-gray-600">{project.outcomes}</p>
    </div>

    <div class="mb-3">
        <h4 class="text-sm font-semibold text-gray-900">Stack:</h4>
        <p class="text-sm text-gray-500 font-mono">{project.stack}</p>
    </div>

    <div class="mt-4 pt-4 border-t border-gray-100 flex justify-between items-center">
        <span class="text-xs text-gray-500 italic">Proof: {project.proof}</span>
        <a href={project.link} class="text-indigo-600 hover:text-indigo-800 text-sm font-medium" target="_blank">View Project &rarr;</a>
    </div>
  </div>
</div>
