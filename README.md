# Daivi - IT & Project Management Portfolio

This is a portfolio website built with [Astro](https://astro.build) and [Tailwind CSS](https://tailwindcss.com).

## 🚀 Project Structure

```text
/
├── public/           # Static assets
├── src/
│   ├── components/   # Reusable UI components (Navbar, Footer, ProjectCard)
│   ├── data/         # Data files (projects.ts)
│   ├── layouts/      # Page layouts
│   └── pages/        # Website pages (Home, About, Projects, Resume)
└── package.json
```

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command                   | Action                                           |
| :------------------------ | :----------------------------------------------- |
| `npm install`             | Installs dependencies                            |
| `npm run dev`             | Starts local dev server at `localhost:4321`      |
| `npm run build`           | Build your production site to `./dist/`          |
| `npm run preview`         | Preview your build locally, before deploying     |

## 🛠 Tech Stack

- **Framework:** Astro
- **Styling:** Tailwind CSS
- **Deployment:** Static Site Generation (SSG)

## 📝 Editing Content

- **Projects:** Edit `src/data/projects.ts` to add or update projects.
- **Pages:** Edit files in `src/pages/` to update content for Home, About, etc.
